# 微服务架构 v1.0

## 1. 项目简介
DayDayUP 是一个基于 Spring Boot 3.5.0、Spring Cloud 2025.0.0 和 Spring Cloud Alibaba 2025.0.0.0 构建的企业级微服务架构项目。
项目集成了后台管理、游戏业务、商业业务、社交业务等多种微服务应用，旨在提供一个高可用、可扩展、易维护的后端服务平台。

## 2. 系统架构

```mermaid
graph TD
    User([用户/客户端]) --> Gateway[网关服务 (Gateway)]
    Gateway --> Auth[认证中心 (Auth)]
    Gateway --> Admin[后台管理 (Admin)]
    Gateway --> Modules[业务模块 (Modules)]
    
    subgraph 平台服务
        Auth
        Admin
    end
    
    subgraph 业务微服务
        Modules --> Biz[通用业务 (Biz)]
        Modules --> Game[游戏服务 (Game)]
        Modules --> Social[社交服务 (Social)]
    end
    
    subgraph 基础设施
        Nacos[Nacos 注册/配置中心]
        RocketMQ[RocketMQ 消息队列]
        Redis[Redis 缓存]
        MySQL[MySQL 数据库]
    end
    
    Gateway -.-> Nacos
    Auth -.-> Nacos
    Admin -.-> Nacos
    Biz -.-> Nacos
    Game -.-> Nacos
    Social -.-> Nacos
```

## 3. 技术栈

| 技术组件 | 版本 | 说明 |
| :--- | :--- | :--- |
| JDK | 21 | 开发语言 |
| Spring Boot | 3.5.0 | 基础框架 |
| Spring Cloud | 2025.0.0 | 微服务规范 |
| Spring Cloud Alibaba | 2025.0.0.0 | 微服务实现 |
| Spring Authorization Server | 1.4.3 | 认证中心 |
| MyBatis Plus | 3.5.10.1 | ORM 框架 |
| Redisson | 3.40.2 | Redis 客户端 |
| RocketMQ | 5.x (Client 2.3.1) | 消息队列 |
| MySQL | 8.3.0 | 数据库 |
| XXL-JOB | 2.4.2 |以此为准 |
| Spring Boot Admin | 3.3.2 | 监控中心 |

## 4. 模块说明

### 4.1 平台服务 (`daydayup-platform`)
提供系统的核心管理能力。
*   **daydayup-admin**: 后台管理系统。采用 `api` (接口定义) 与 `biz` (业务实现) 分离的架构。
*   **daydayup-auth**: 统一认证中心。基于 Spring Authorization Server，负责 Token 发放与校验。

### 4.2 接入层 (`daydayup-access`)
*   **daydayup-gateway**: 统一网关。负责路由转发、鉴权、限流与熔断。

### 4.3 业务微服务 (`daydayup-modules`)
负责具体的垂直业务领域，均采用 `api` + `biz` 分离架构。
*   **daydayup-biz**: 通用商业业务服务。
*   **daydayup-game**: 游戏相关业务服务。
*   **daydayup-social**: 社交相关业务服务。

### 4.4 公共组件 (`daydayup-common`)
提供通用的工具与基础能力。
*   `daydayup-common-core`: 核心工具类、常量、异常定义。
*   `daydayup-common-web`: Web 开发通用配置、全局异常处理。
*   `daydayup-common-mybatis`: MyBatis Plus 配置与封装。
*   `daydayup-common-security`: 安全配置与资源服务器适配。
*   `daydayup-common-redis`: Redis/Redisson 缓存配置。
*   `daydayup-common-mq`: RocketMQ 消息队列封装。
*   `daydayup-common-swagger`: 接口文档配置。

## 5. 目录结构

```text
daydayup
├── daydayup-access          # 接入层
│   └── daydayup-gateway     # 网关服务
├── daydayup-common          # 公共模块
│   ├── daydayup-common-core
│   ├── daydayup-common-security
│   └── ...
├── daydayup-modules         # 业务模块
│   ├── daydayup-biz         # 商业业务 (api/biz)
│   ├── daydayup-game        # 游戏业务 (api/biz)
│   └── daydayup-social      # 社交业务 (api/biz)
├── daydayup-platform        # 平台服务
│   ├── daydayup-admin       # 后台管理 (api/biz)
│   └── daydayup-auth        # 认证中心
├── sql                      # 初始化脚本
├── pom.xml                  # 父工程依赖管理
└── 微服务架构v1.md           # 架构文档
```

## 6. 开发规范

1.  **分层架构**：业务模块强制执行 API 与 Biz 分离。
    *   `*-api`: 包含 DTO, VO, Feign Client 接口。可被其他服务依赖。
    *   `*-biz`: 包含 Entity, Service, Controller。
2.  **代码注释**：所有类、方法必须包含详细的中文注释（Javadoc 格式）。
3.  **工具类**：优先使用 `daydayup-common` 提供的工具，避免重复造轮子。
4.  **接口文档**：使用 SpringDoc/Swagger 注解描述接口。
